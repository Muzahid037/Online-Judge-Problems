#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
map<ll,ll>mp;
int main()
{
    ll n,p;
    cin>>n;
    ll enc[n+10];
    ll key[n+10];
    ll real[n+10];
    for(ll i=0; i<n; i++)
    {
        cin>>enc[i];
        real[i]=1;
    }
    for(ll i=0; i<n; i++)
    {
        cin>>key[i];
    }

    for(ll k=0; k<n; k++)
    {
        ll tmpkey=key[k];
        ll cntCst=0;
        for(ll i=n-1; i>=0; i--)
        {
            if()
            if(tmpkey==cntCst)
            {

            }

        }
    }

    return 0;
}

